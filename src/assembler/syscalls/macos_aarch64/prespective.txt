0 = invalid
// exits the program execution with return code `return_value`
1 = exit(return_value: i16) -> nothing
// TODO: ???
2 = fork(nothing) -> i16
// reads between 0 and `buffer_size` bytes from `file_descriptor` into `buffer`
// returns the number of bytes read, or -1 if this failed
3 = read(file_descriptor: i16, buffer: ptr, buffer_size: usize) -> isize
// writes between 0 and `buffer_size` bytes from `buffer` into `file_descriptor`
// returns the number of bytes writter, or -1 if this failed
4 = write(file_descriptor: i16, buffer: ptr, buffer_size: usize) -> isize

0 nosys
5 open
6 sys_close
7 wait4
8 enosys
9 link
10 unlink
11 enosys
12 chdir
13 fchdir
14 mknod
15 chmod

user_size_t ~> size_t => usize
user_addr_t ~> const void * => ptr
user_ssize_t ~> ssize_t => isize
