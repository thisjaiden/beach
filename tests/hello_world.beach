// Narrows platforms by support for stdout
system stdout;

// Creates an alias: print refers to stdout
print => stdout;

// Runs on program start
main {
    // Print "Hello world!" to stdout (usually a console of some sort)
    print("Hello world!");

    // End the program
    return;
}
